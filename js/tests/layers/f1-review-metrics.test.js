// ============================================================
// F1 — REVIEW METRICS
// ============================================================

(function () {
  "use strict";

  const {
    test,
    assertEqual,
    assertExists,
  } = window.ArtworkTests;

  test(
    "F1 pending metric matches appState",
    () => {
      // futuro teste F1
    },
  );

  test(
    "F1 approved metric matches appState",
    () => {
      // futuro teste F1
    },
  );
})();